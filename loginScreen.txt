1. Return type bool
2. first call loginScreenMenu() - func just to display user options
3. ask for input:
-1. sign in,
-2. sign up,
-3. exit,
4. if -1 call fuction for signing in with return type bool
5. if true pass true to return of loginScreenMenu
6. if false let user try again.
7. if -2 call functions for signing up which will add User to userList via pointers and store changes in file.
8. if -3 pass false to return of loginScreenMenu

*probably will have to add -3 changer user name -4 change user password -5 exit shifted here.